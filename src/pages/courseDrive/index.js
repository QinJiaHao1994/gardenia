import Container from "@mui/material/Container";
import Box from "@mui/material/Box";

const CourseDrive = () => {
  return (
    <Container maxWidth="lg">
      <Box sx={{ mt: 4 }}>CourseDrive</Box>
    </Container>
  );
};

export default CourseDrive;
